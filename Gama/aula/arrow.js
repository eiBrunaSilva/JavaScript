function soma(x, y) {
  return x + y;
}

const multi = function (x, y) {
  return x * y;
};

const dividir = (x, y) => {
  return x / y;
};

const objeto = () => ({ nome: "Bruna", sobrenome: "Silva" });
